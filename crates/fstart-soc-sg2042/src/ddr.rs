//! SG2042 DDR memory controller driver (stub).
//!
//! The production DDR init code (`mango_ddr_init_asic`) is a ~100 KB
//! closed-source blob pending reverse engineering. This stub satisfies
//! the MemoryController interface required by the `DramInit` capability
//! and halts the system with a clear diagnostic message.
