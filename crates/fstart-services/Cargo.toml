[package]
name = "fstart-services"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
default = []
pci = ["dep:fstart-pci"]

[dependencies]
embedded-hal = { workspace = true }
fstart-pci = { workspace = true, optional = true }
fstart-types = { workspace = true }
