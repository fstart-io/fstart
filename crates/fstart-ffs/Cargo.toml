[package]
name = "fstart-ffs"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
default = []
std = ["fstart-types/std", "postcard/use-std"]
alloc = ["fstart-types/alloc", "postcard/alloc"]

[dependencies]
fstart-types = { workspace = true }
postcard = { workspace = true }
