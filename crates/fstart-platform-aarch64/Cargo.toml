[package]
name = "fstart-platform-aarch64"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
fstart-soc-sunxi = { workspace = true, optional = true }

[features]
default = []
sunxi = ["dep:fstart-soc-sunxi"]
sbsa = []
# Sophgo SG2042 (Milk-V Pioneer): correct MMU page tables for the 40-bit
# peripheral address space. No extra runtime dependency needed.
sophgo = []
