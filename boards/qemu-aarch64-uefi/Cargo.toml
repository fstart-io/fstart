[package]
name = "fstart-board-qemu-aarch64-uefi"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[package.metadata.fstart]
board = "qemu-aarch64-uefi"
platform = "aarch64"
target = "aarch64-unknown-none"
stage-package = "fstart-board-qemu-aarch64-uefi-stage"
stage-bin = "fstart-stage"

[dependencies]
fstart-board-qemu-aarch64-uefi-facts = { path = "facts" }
fstart-types = { workspace = true, features = ["std"] }
