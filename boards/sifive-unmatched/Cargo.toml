[package]
name = "fstart-board-sifive-unmatched"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[package.metadata.fstart]
board = "sifive-unmatched"
platform = "riscv64"
target = "riscv64gc-unknown-none-elf"
stage-package = "fstart-board-sifive-unmatched-stage"
stage-bin = "fstart-stage"

[dependencies]
fstart-board-sifive-unmatched-facts = { path = "facts" }
fstart-types = { workspace = true, features = ["std"] }
