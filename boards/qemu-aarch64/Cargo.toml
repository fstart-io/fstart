[package]
name = "fstart-board-qemu-aarch64"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[package.metadata.fstart]
board = "qemu-aarch64"
platform = "aarch64"
target = "aarch64-unknown-none"
stage-bin = "fstart-stage"
stage-recipe = "qemu-virt-linux"
[features]
default = []
stage = [
  "dep:fstart-stage",
  "dep:fstart-services",
  "dep:fstart-platform-aarch64",
  "dep:fstart-driver-pl011",
  "fstart-stage/ffs",
  "fstart-stage/fdt",
]

[dependencies]
fstart-board-qemu-virt = { workspace = true }
fstart-types = { workspace = true }
fstart-stage = { workspace = true, optional = true }
fstart-services = { workspace = true, optional = true }
fstart-platform-aarch64 = { workspace = true, optional = true }
fstart-driver-pl011 = { workspace = true, optional = true }
