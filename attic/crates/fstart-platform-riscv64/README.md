# Unported remainder

`src/entry_sunxi.rs` is D1/T113-specific RISC-V reset capital. It remains in
the attic until a Sunxi RISC-V platform flow returns; QEMU RISC-V entry code
now lives in `crates/fstart-arch/src/riscv64.rs`.
